document.addEventListener("DOMContentLoaded",function(){document.getElementById("form-sorteador").addEventListener("submit",function(evento){evento.preventDefault();evento=document.getElementById("numero-maximo").value,evento=parseInt(evento),evento=Math.random()*evento,evento=Math.floor(1+evento);document.getElementById("resultado-valor").innerText=evento,document.querySelector(".resultado").style.display="block"})});